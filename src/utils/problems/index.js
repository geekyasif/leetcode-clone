import { twoSum } from "./two_sum";
import { reverseLinkedList } from "./reverse_linkedlist";

export const ProblemList = [
    { 
        title: twoSum.title, 
        id: twoSum.id,
        problem: twoSum
    },
    { 
        title: reverseLinkedList.title, 
        id: reverseLinkedList.id,
        problem: reverseLinkedList
    }
]